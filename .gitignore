__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
*.egg-info/
dist/
build/

# OAuth client secrets downloaded for get_gmail_token.py — must never be committed
credentials.json
client_secret*.json
token.json

# Resumable historical-backfill checkpoint (local progress; never commit)
.backfill_checkpoint.json
*.checkpoint.json

